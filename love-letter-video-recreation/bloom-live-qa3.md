# Bloom click QA

The continuation cue successfully lands on `#bloom` and shows the loading transition before revealing “CLICK TO BLOOM”. Activating the button succeeds in the live DOM and triggers the final bloom state in the same page flow.
