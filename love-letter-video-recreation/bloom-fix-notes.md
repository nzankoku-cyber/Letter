# Bloom correction notes

After the garden-window patch, the invitation still opens into the existing love-letter flow and the bloom section remains reachable from the continuation cue. The corrected scene is being verified through the live preview before the final checkpoint.

The ready state now presents a visible rectangular garden window with a contained night texture before the flower animation is released. The CLICK TO BLOOM control remains above the window and the hint remains below it.

The completed-state inspection showed the supplied animation progressing from short glowing stems to full flower heads, layered leaves, and foreground grasses. The composition is now being tightened further with a smaller inner scale and a near-bottom baseline so the tallest foliage stays inside the framed window.

Computed live styles confirm the flower-head animation is `supplied-blooming-flower` with `animation-play-state: running`, and the stem animation is `supplied-grow-flower-tree`. The final desktop capture shows the visible grasses and ground foliage clipped within the framed square.

The final screenshot pass confirms the invitation cover remains readable and usable at both 1280px desktop and 375px mobile widths after the bloom layout changes.
