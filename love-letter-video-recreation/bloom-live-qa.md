# Bloom live QA

The invitation cover opens correctly, audio starts on the user gesture, and the new final section is visible in its loading state with “preparing something soft...”. The live preview still exposes the letter continuation cue as `href="#proposal"`; the new final section is now `id="bloom"`, so this anchor must be updated before final verification.
